'use strict';

const categoryDelete = require('./categoryDelete');
const categoryEdit = require('./categoryEdit');
const categoryGet = require('./categoryGet');
const categoryList = require('./categoryList');
const categoryNew = require('./categoryNew');
const categoryPhotoUpload = require('./categoryPhotoUpload');

module.exports = {
    categoryDelete,
    categoryEdit,
    categoryGet,
    categoryList,
    categoryNew,
    categoryPhotoUpload,
};
